// OpenNext adapter config for deploying this Next.js app to Cloudflare Workers.
// The defaults are right for this site; caching/queue overrides can be added
// here later if needed. See https://opennext.js.org/cloudflare
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({});
